// ============================================
// CREASHIFT - CORPORATE PROFESSIONAL
// Futuristic Animations & Interactions
// ============================================

document.addEventListener('DOMContentLoaded', () => {
    initTheme();
    initNavigation();
    initParticles(); // Branded hero animation
    initServiceCardsReveal();
    initFloatingIcons(); // Service floating icons
    initStatsCounters();
    initProcessTimeline();
    initContactForm();
});


// ============================================
// THEME MANAGEMENT
// ============================================
function initTheme() {
    const themeToggle = document.getElementById('theme-toggle');
    const body = document.body;

    const savedTheme = localStorage.getItem('theme');
    const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;

    if (savedTheme) {
        body.setAttribute('data-theme', savedTheme);
    } else if (systemPrefersDark) {
        body.setAttribute('data-theme', 'dark');
    } else {
        body.setAttribute('data-theme', 'light');
    }

    themeToggle.addEventListener('click', () => {
        const currentTheme = body.getAttribute('data-theme');
        const newTheme = currentTheme === 'dark' ? 'light' : 'dark';

        body.style.transition = 'background 0.3s ease, color 0.3s ease';
        body.setAttribute('data-theme', newTheme);
        localStorage.setItem('theme', newTheme);

        setTimeout(() => {
            body.style.transition = '';
        }, 300);
    });
}

// ============================================
// SIDE NAVIGATION
// ============================================
function initSideNav() {
    const navLinks = document.querySelectorAll('.sidenav-link');

    // Smooth scroll for internal links
    navLinks.forEach(link => {
        link.addEventListener('click', (e) => {
            const href = link.getAttribute('href');

            // Only handle internal section links
            if (href.startsWith('#')) {
                e.preventDefault();

                // Special handling for home link
                if (href === '#home' || href === '#') {
                    window.scrollTo({
                        top: 0,
                        behavior: 'smooth'
                    });
                } else {
                    const targetSection = document.querySelector(href);
                    if (targetSection) {
                        targetSection.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                }

                // Update active state
                navLinks.forEach(l => l.classList.remove('active'));
                link.classList.add('active');
            }
        });
    });

    // Update active on scroll
    const sections = document.querySelectorAll('section[id]');
    window.addEventListener('scroll', () => {
        let current = '';
        sections.forEach(section => {
            const sectionTop = section.offsetTop;
            const sectionHeight = section.clientHeight;
            if (window.scrollY >= sectionTop - 200) {
                current = section.getAttribute('id');
            }
        });

        navLinks.forEach(link => {
            link.classList.remove('active');
            if (link.getAttribute('data-section') === current) {
                link.classList.add('active');
            }
        });
    }, { passive: true });
}

// ============================================
// SCROLL PROGRESS BAR
// ============================================
function initScrollProgress() {
    const progressBar = document.getElementById('progress-bar');

    window.addEventListener('scroll', () => {
        const windowHeight = window.innerHeight;
        const documentHeight = document.documentElement.scrollHeight - windowHeight;
        const scrolled = window.scrollY;
        const progress = (scrolled / documentHeight) * 100;

        progressBar.style.width = progress + '%';
    }, { passive: true });
}

// ============================================
// BRANDED HERO ANIMATION - Digital Marketing Viz
// ============================================
function initParticles() {
    const canvas = document.getElementById('hero-grid');
    if (!canvas) return;

    const ctx = canvas.getContext('2d');
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    // Creashift brand colors from logo
    const brandColors = {
        purple: 'rgba(138, 43, 226, ',
        indigo: 'rgba(75, 0, 130, ',
        blue: 'rgba(65, 105, 225, ',
        accent: 'rgba(147, 51, 234, '
    };

    // Data points for network
    const particles = [];
    const particleCount = 40;

    // Animated graph bars (digital marketing metrics)
    const metricBars = [];
    for (let i = 0; i < 8; i++) {
        metricBars.push({
            x: 100 + i * 150,
            y: canvas.height - 100,
            height: Math.random() * 200 + 50,
            targetHeight: Math.random() * 200 + 50,
            speed: 0.02
        });
    }

    // Rising data streams
    const dataStreams = [];
    for (let i = 0; i < 15; i++) {
        dataStreams.push({
            x: Math.random() * canvas.width,
            y: canvas.height + Math.random() * 100,
            speed: 0.5 + Math.random() * 1,
            opacity: Math.random() * 0.3 + 0.1
        });
    }

    class Particle {
        constructor() {
            this.x = Math.random() * canvas.width;
            this.y = Math.random() * canvas.height;
            this.vx = (Math.random() - 0.5) * 0.5;
            this.vy = (Math.random() - 0.5) * 0.5;
            this.size = Math.random() * 3 + 2;
            this.pulse = Math.random() * Math.PI * 2;
        }

        update() {
            this.x += this.vx;
            this.y += this.vy;
            this.pulse += 0.02;

            if (this.x < 0 || this.x > canvas.width) this.vx *= -1;
            if (this.y < 0 || this.y > canvas.height) this.vy *= -1;
        }

        draw() {
            const pulseSize = this.size + Math.sin(this.pulse) * 1.5;
            ctx.beginPath();
            ctx.arc(this.x, this.y, pulseSize, 0, Math.PI * 2);
            ctx.fillStyle = brandColors.purple + '0.6)';
            ctx.fill();

            // Glow effect
            ctx.beginPath();
            ctx.arc(this.x, this.y, pulseSize * 1.5, 0, Math.PI * 2);
            ctx.fillStyle = brandColors.accent + '0.2)';
            ctx.fill();
        }
    }

    for (let i = 0; i < particleCount; i++) {
        particles.push(new Particle());
    }

    function animate() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);

        // Draw animated metric bars
        metricBars.forEach(bar => {
            // Animate height changes
            if (Math.abs(bar.height - bar.targetHeight) > 1) {
                bar.height += (bar.targetHeight - bar.height) * bar.speed;
            } else {
                bar.targetHeight = Math.random() * 200 + 50;
            }

            // Gradient bar
            const gradient = ctx.createLinearGradient(0, bar.y, 0, bar.y - bar.height);
            gradient.addColorStop(0, brandColors.purple + '0.3)');
            gradient.addColorStop(1, brandColors.blue + '0.6)');

            ctx.fillStyle = gradient;
            ctx.fillRect(bar.x, bar.y, 40, -bar.height);

            // Bar border
            ctx.strokeStyle = brandColors.accent + '0.8)';
            ctx.lineWidth = 2;
            ctx.strokeRect(bar.x, bar.y, 40, -bar.height);
        });

        // Draw rising data streams
        dataStreams.forEach(stream => {
            stream.y -= stream.speed;
            if (stream.y < -20) {
                stream.y = canvas.height + 20;
                stream.x = Math.random() * canvas.width;
            }

            ctx.fillStyle = brandColors.indigo + stream.opacity + ')';
            ctx.fillRect(stream.x, stream.y, 2, 20);
        });

        // Update and draw particles
        particles.forEach(particle => {
            particle.update();
            particle.draw();
        });

        // Draw connections (network effect)
        for (let i = 0; i < particles.length; i++) {
            for (let j = i + 1; j < particles.length; j++) {
                const dx = particles[i].x - particles[j].x;
                const dy = particles[i].y - particles[j].y;
                const distance = Math.sqrt(dx * dx + dy * dy);

                if (distance < 150) {
                    ctx.beginPath();
                    ctx.moveTo(particles[i].x, particles[i].y);
                    ctx.lineTo(particles[j].x, particles[j].y);
                    const opacity = 0.3 * (1 - distance / 150);
                    ctx.strokeStyle = brandColors.blue + opacity + ')';
                    ctx.lineWidth = 1;
                    ctx.stroke();
                }
            }
        }

        requestAnimationFrame(animate);
    }

    animate();

    // Resize handler
    window.addEventListener('resize', () => {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
    });
}

// ============================================
// SERVICE CARDS SCROLL REVEAL
// ============================================
function initServiceCardsReveal() {
    const serviceCards = document.querySelectorAll('.service-card-new');

    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry, index) => {
            if (entry.isIntersecting) {
                setTimeout(() => {
                    entry.target.classList.add('revealed');
                }, index * 100);
            }
        });
    }, { threshold: 0.2 });

    serviceCards.forEach(card => observer.observe(card));
}

// ============================================
// FLOATING INTERACTIVE ICONS
// ============================================
function initFloatingIcons() {
    const container = document.getElementById('floating-icons');
    if (!container) return;

    const services = [
        {
            icon: '📈',
            name: 'Digital Strategy',
            desc: 'Comprehensive roadmaps that align your digital initiatives with business objectives.'
        },
        {
            icon: '🎨',
            name: 'Brand Development',
            desc: 'Build powerful brand identities that resonate with your target audience.'
        },
        {
            icon: '💻',
            name: 'Web Solutions',
            desc: 'Enterprise-grade websites and applications built with modern technology.'
        },
        {
            icon: '🚀',
            name: 'Digital Marketing',
            desc: 'Data-driven campaigns that deliver measurable ROI and business growth.'
        },
        {
            icon: '📊',
            name: 'Analytics & Insights',
            desc: 'Transform data into actionable insights for strategic decision-making.'
        },
        {
            icon: '💼',
            name: 'Consulting',
            desc: 'Expert guidance to optimize your digital transformation journey.'
        },
        {
            icon: '⚡',
            name: 'Optimization',
            desc: 'Continuous performance tuning for peak digital efficiency.'
        },
        {
            icon: '🔒',
            name: 'Cyber Security',
            desc: 'Robust protection strategies for your valuable digital assets.'
        }
    ];

    services.forEach((service, i) => {
        const iconEl = document.createElement('div');
        iconEl.className = 'floating-icon';
        iconEl.innerHTML = `
      <div class="icon-sphere">${service.icon}</div>
      <div class="service-popup">
        <h4>${service.name}</h4>
        <p>${service.desc}</p>
        <div class="popup-cta">Explore More →</div>
      </div>
    `;

        // Increased scatter for better coverage
        const posX = 10 + Math.random() * 80;
        const posY = 10 + Math.random() * 80;
        iconEl.style.left = `${posX}%`;
        iconEl.style.top = `${posY}%`;

        // Even slower drift for better readability
        let angle = Math.random() * Math.PI * 2;
        let speed = 0.02 + Math.random() * 0.04;
        let x = posX;
        let y = posY;

        function drift() {
            if (iconEl.classList.contains('hovered')) return requestAnimationFrame(drift);

            x += Math.cos(angle) * speed;
            y += Math.sin(angle) * speed;

            // Bounce off walls with margin for the popup
            if (x < 5 || x > 90) angle = Math.PI - angle;
            if (y < 5 || y > 90) angle = -angle;

            iconEl.style.left = `${x}%`;
            iconEl.style.top = `${y}%`;

            requestAnimationFrame(drift);
        }

        iconEl.addEventListener('mouseenter', () => {
            iconEl.classList.add('hovered');
        });

        iconEl.addEventListener('mouseleave', () => {
            iconEl.classList.remove('hovered');
        });

        container.appendChild(iconEl);
        drift();
    });
}

// ============================================
// ANIMATED STATS COUNTERS
// ============================================
function initStatsCounters() {
    const statCards = document.querySelectorAll('.stat-card');

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting && !entry.target.classList.contains('animated')) {
                entry.target.classList.add('animated');

                const numberElement = entry.target.querySelector('.stat-number');
                const target = parseInt(numberElement.getAttribute('data-target'));
                animateCounter(numberElement, target);
            }
        });
    }, { threshold: 0.5 });

    statCards.forEach(card => observer.observe(card));
}

function animateCounter(element, target) {
    let current = 0;
    const increment = target / 60; // 60 frames
    const duration = 2000; // 2 seconds
    const stepTime = duration / 60;

    const timer = setInterval(() => {
        current += increment;
        if (current >= target) {
            element.textContent = target;
            clearInterval(timer);
        } else {
            element.textContent = Math.floor(current);
        }
    }, stepTime);
}

// ============================================
// TIMELINE REVEAL ANIMATION
// ============================================
function initTimelineReveal() {
    const timelineItems = document.querySelectorAll('.timeline-item');

    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry, index) => {
            if (entry.isIntersecting) {
                setTimeout(() => {
                    entry.target.classList.add('revealed');
                }, index * 200);
            }
        });
    }, { threshold: 0.3 });

    timelineItems.forEach(item => observer.observe(item));
}

// ============================================
// CONTACT PARTICLES BACKGROUND
// ============================================
function initContactParticles() {
    const canvas = document.getElementById('contact-particles');
    if (!canvas) return;

    const ctx = canvas.getContext('2d');
    const particles = [];

    function resizeCanvas() {
        canvas.width = canvas.offsetWidth;
        canvas.height = canvas.offsetHeight;
    }
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);

    class FloatingParticle {
        constructor() {
            this.x = Math.random() * canvas.width;
            this.y = Math.random() * canvas.height;
            this.size = Math.random() * 3 + 1;
            this.speedY = Math.random() * 0.5 + 0.2;
            this.opacity = Math.random() * 0.5 + 0.2;
        }

        update() {
            this.y -= this.speedY;
            if (this.y < 0) {
                this.y = canvas.height;
                this.x = Math.random() * canvas.width;
            }
        }

        draw() {
            ctx.beginPath();
            ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
            const colors = ['rgba(126, 34, 206, 0.7)', 'rgba(79, 70, 229, 0.7)', 'rgba(30, 27, 75, 0.7)'];
            const colorIndex = Math.floor(Math.random() * colors.length);
            ctx.fillStyle = colors[colorIndex].replace('0.7', this.opacity);
            ctx.fill();
        }
    }

    for (let i = 0; i < 50; i++) {
        particles.push(new FloatingParticle());
    }

    function animate() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);

        particles.forEach(particle => {
            particle.update();
            particle.draw();
        });

        requestAnimationFrame(animate);
    }

    animate();
}

// ============================================
// CONTACT FORM
// ============================================
function initContactForm() {
    const form = document.getElementById('contact-form');

    if (form) {
        form.addEventListener('submit', (e) => {
            e.preventDefault();

            const name = document.getElementById('form-name').value.trim();
            const email = document.getElementById('form-email').value.trim();
            const company = document.getElementById('form-company').value.trim();
            const message = document.getElementById('form-message').value.trim();

            if (!name || !email || !company || !message) {
                showFormMessage('Please fill in all fields.', 'error');
                return;
            }

            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                showFormMessage('Please enter a valid email address.', 'error');
                return;
            }

            const submitButton = document.getElementById('form-submit');
            submitButton.disabled = true;
            submitButton.innerHTML = '<span>Sending...</span>';

            // Simulate submission
            setTimeout(() => {
                showFormMessage('Thank you! We will contact you soon.', 'success');
                form.reset();
                submitButton.disabled = false;
                submitButton.innerHTML = '<span>Send Message</span><span class="btn-arrow">→</span>';

                // Success particle burst
                createSuccessParticles();
            }, 1500);
        });
    }
}

function showFormMessage(message, type) {
    const existingMessage = document.querySelector('.form-message');
    if (existingMessage) {
        existingMessage.remove();
    }

    const messageDiv = document.createElement('div');
    messageDiv.className = `form - message form - message - ${type} `;
    messageDiv.textContent = message;
    messageDiv.style.cssText = `
padding: 1rem;
margin - top: 1rem;
border - radius: 12px;
text - align: center;
font - weight: 500;
background: ${type === 'success' ? 'rgba(34, 197, 94, 0.1)' : 'rgba(239, 68, 68, 0.1)'};
border: 1px solid ${type === 'success' ? 'rgba(34, 197, 94, 0.3)' : 'rgba(239, 68, 68, 0.3)'};
color: ${type === 'success' ? '#22c55e' : '#ef4444'};
animation: fadeInUp 0.3s ease - out;
`;

    const form = document.getElementById('contact-form');
    form.appendChild(messageDiv);

    setTimeout(() => {
        messageDiv.style.opacity = '0';
        messageDiv.style.transform = 'translateY(-10px)';
        setTimeout(() => messageDiv.remove(), 300);
    }, 5000);
}

function createSuccessParticles() {
    const form = document.getElementById('contact-form');
    const rect = form.getBoundingClientRect();

    for (let i = 0; i < 20; i++) {
        const particle = document.createElement('div');
        particle.style.cssText = `
position: fixed;
left: ${rect.left + rect.width / 2} px;
top: ${rect.top + rect.height / 2} px;
width: 8px;
height: 8px;
background: linear - gradient(135deg, #7E22CE, #4F46E5, #1E1B4B);
border - radius: 50 %;
pointer - events: none;
z - index: 9999;
`;
        document.body.appendChild(particle);

        const angle = (Math.PI * 2 * i) / 20;
        const velocity = 3;
        const vx = Math.cos(angle) * velocity;
        const vy = Math.sin(angle) * velocity;

        let x = 0;
        let y = 0;
        let opacity = 1;

        function animateParticle() {
            x += vx;
            y += vy;
            opacity -= 0.02;

            particle.style.transform = `translate(${x}px, ${y}px)`;
            particle.style.opacity = opacity;

            if (opacity > 0) {
                requestAnimationFrame(animateParticle);
            } else {
                particle.remove();
            }
        }

        animateParticle();
    }
}

// ============================================
// CTA BUTTON INTERACTIONS
// ============================================
const ctaPrimary = document.getElementById('hero-cta-primary');
const ctaSecondary = document.getElementById('hero-cta-secondary');

if (ctaPrimary) {
    ctaPrimary.addEventListener('click', () => {
        document.querySelector('#contact').scrollIntoView({
            behavior: 'smooth'
        });
    });
}

if (ctaSecondary) {
    ctaSecondary.addEventListener('click', () => {
        document.querySelector('#services').scrollIntoView({
            behavior: 'smooth'
        });
    });
}

 / /   = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = 
 / /   I N I T I A L I Z A T I O N   -   R u n   a l l   f u n c t i o n s   o n   l o a d 
 / /   = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = 
 d o c u m e n t . a d d E v e n t L i s t e n e r ( ' D O M C o n t e n t L o a d e d ' ,   ( )   = >   { 
         i n i t T h e m e ( ) ; 
         i n i t N a v i g a t i o n ( ) ; 
         i n i t P a r t i c l e s ( ) ; 
         i n i t F l o a t i n g I c o n s ( ) ; 
         i n i t S t a t s C o u n t e r s ( ) ; 
         i n i t P r o c e s s T i m e l i n e ( ) ; 
         i n i t C o n t a c t F o r m ( ) ; 
         i n i t S e r v i c e C a r d s R e v e a l ( ) ; 
 
} ) ; 
 
 
 